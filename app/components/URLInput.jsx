import React from 'react';

export default React.createClass({
    render: function () {
        return (
            <input
                className="url-input"
                type="text"
                placeholder="Paste a link" />
        );
    }
});
